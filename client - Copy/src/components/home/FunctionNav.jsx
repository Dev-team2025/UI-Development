/* functional component is a type of react components 
which are similar to javascript functions
before react-18 function component is known as state less component
hooks: these are the concepts that we are have in functional component to make them statefull 
useState hook
useEffect hook

shortcut for functional template : rfc,rfce,rafce
 */
import React from 'react'

function FunctionNav() {
    return (
        <div>
            <h1>hello</h1>
        </div>
    )
}

export default FunctionNav
